<?php
require_once PATH_TRUNK . 'gulliver/thirdparty/smarty/libs/Smarty.class.php';
require_once PATH_TRUNK . 'gulliver/system/class.xmlform.php';
require_once PATH_TRUNK . 'gulliver/system/class.xmlDocument.php';
require_once PATH_TRUNK . 'gulliver/system/class.form.php';
require_once PATH_TRUNK . 'gulliver/system/class.dbconnection.php';
require_once PATH_TRUNK . 'gulliver/thirdparty/propel/Propel.php';
require_once PATH_TRUNK . 'gulliver/thirdparty/creole/Creole.php';
require_once PATH_TRUNK . 'gulliver/thirdparty/pear/PEAR.php';
require_once PATH_TRUNK . 'workflow/engine/classes/class.xmlDb.php';

/**
 * Generated by ProcessMaker Test Unit Generator on 2012-07-12 at 22:32:32.
*/

class classXMLDBTest extends PHPUnit_Framework_TestCase
{
    /**
     * @var XMLDB
    */
    protected $object;

    /**
     * Sets up the fixture, for example, opens a network connection.
     * This method is called before a test is executed.
    */
    protected function setUp()
    {
        $this->object = new XMLDB();
    }

    /**
     * Tears down the fixture, for example, closes a network connection.
     * This method is called after a test is executed.
    */
    protected function tearDown()
    {
    }

    /**
     * This is the default method to test, if the class still having 
     * the same number of methods.
    */
    public function testNumberOfMethodsInThisClass()
    {
        $methods = get_class_methods('XMLDB');
        $this->assertTrue( count($methods) == 2);
    }

    /**
    * @covers XMLDB::connect
    * @todo   Implement testconnect().
    */
    public function testconnect()
    {
        $methods = get_class_methods($this->object);
        $this->assertTrue( in_array('connect', $methods ), 'exists method connect' );
        $r = new ReflectionMethod('XMLDB', 'connect');
        $params = $r->getParameters();
        $this->assertTrue( $params[0]->getName() == 'dsn');
        $this->assertTrue( $params[0]->isArray() == false);
        $this->assertTrue( $params[0]->isOptional () == false);
        $this->assertTrue( $params[1]->getName() == 'options');
        $this->assertTrue( $params[1]->isArray() == false);
        $this->assertTrue( $params[1]->isOptional () == true);
        $this->assertTrue( $params[1]->getDefaultValue() == array());
    }

    /**
    * @covers XMLDB::isError
    * @todo   Implement testisError().
    */
    public function testisError()
    {
        $methods = get_class_methods($this->object);
        $this->assertTrue( in_array('isError', $methods ), 'exists method isError' );
        $r = new ReflectionMethod('XMLDB', 'isError');
        $params = $r->getParameters();
        $this->assertTrue( $params[0]->getName() == 'result');
        $this->assertTrue( $params[0]->isArray() == false);
        $this->assertTrue( $params[0]->isOptional () == false);
    }
}

