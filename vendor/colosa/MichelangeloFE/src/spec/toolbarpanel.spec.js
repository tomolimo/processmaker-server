
describe("ToolbarPanel Class", function () {
    it("Should be an object", function () {
        var toolBarPanel = new ToolbarPanel();
        expect(typeof toolBarPanel).toEqual("object");
    });
});